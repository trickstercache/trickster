discovery:
  in-cluster:
    provider: kubernetes
backends:
  prom-template:
    provider: prometheus
    is_template: true
  test-alb:
    provider: alb
    alb:
      mechanism: rr
      discovery:
        discoverer_name: missing
        template_backend: prom-template
        query:
          service: prom
